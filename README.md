# Teamspeak Setup Docker
#
# Bevor die docker-compose.yaml ausgeführt wird muss dieser Befehl abgesetzt werden.

# echo "mein_sicheres_passwort" | openssl enc -aes-256-cbc -a -salt -out secrets/mysql_root_password.enc
